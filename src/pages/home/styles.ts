import { styled } from '@og-ui/react'

export const HomePageContainer = styled('main', {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',

  width: '100%',
  height: '100vh',
})
